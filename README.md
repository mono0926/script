- 様々なスクリプトの置き場
- `bin/`に目的ごとにファイルを分けて配置
  - 複雑な場合や共通処理以外では`lib/`は使わない
- 1まとまりの作業完了する度に、自動的にmainブランチに直接コミット・PUSH
- スクリプトの実行やタスク管理には [Task](https://taskfile.dev/) を使用 (`Taskfile.yaml` に定義)
- **publicリポジトリとして問題ないように保つため、秘匿情報はコミットしない**
